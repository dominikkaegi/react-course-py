import { useHistory } from "react-router-dom";

function ProframmaticRedirection() {
  const history = useHistory();
  const handleClick = () => {};

  return <button onClick={handleClick}>Redirect</button>;
}
